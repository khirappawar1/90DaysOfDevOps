apiVersion: v1
kind: Pod
metadata:
  name: readiness-demo-pod
  labels:
    app: nginx 
spec:
  containers:
  - name: nginx
    image: nginx
    readinessProbe:
      httpGet:
        path: /
        port: 80
      initialDelaySeconds: 5
      periodSeconds: 5
      failureThreshold: 3